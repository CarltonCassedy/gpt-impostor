


// Sends training job to TPUs. Will fail if server is already training something.
function sendTrainingData (guildId, messages) {

}

// Asks training server about the training status of guildId
// Returns either training, not training, or complete.
function pollTrainingServer(guildId) {

}
